# Configuring-DHCP-on-Wireless-Router
I have enabled the DHCP server in three PCs and then went to the web by using the default gateway to change default DHCP setting to a specific network range and to configure the clients to obtain their adress via the DHCP.
